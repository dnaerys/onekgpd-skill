[pytest]
testpaths = .
addopts = -ra
markers =
    live: requires the live 1000 Genomes endpoint; auto-skipped if unreachable
    unit: opt-in in-process unit tests that import dnaerys (collected only with --run-unit)
